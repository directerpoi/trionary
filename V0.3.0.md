You are a compiler engineer and language architect.

I have built Trionary v0.2.0 with:

- Arithmetic execution
- Variables
- Pipeline system (lst, whn, trn, sum, emt)
- Variable support inside pipeline transforms
- CLI execution (tri run file.tri)
- Optional CLI input (arg0, arg1, ...)

The execution model is:
Single-pass pipeline with minimal memory and high performance.

---

GOAL FOR v0.3.0

Evolve the language toward real-world usability WITHOUT breaking simplicity or performance.

---

DESIGN PRINCIPLES

- Keep syntax minimal and English-like
- Maintain strict grammar (no free-form language)
- Preserve single-pass execution model
- Avoid overengineering
- Backward compatibility is mandatory

---

PROPOSED FEATURES (CANDIDATES)

1. Function system
   Example:
   fn add x y
     x + y
   end

2. Module system
   Example:
   use math
   use io

3. CLI input improvements
   (arg0, arg1 already supported → extend usability)

4. Better error system
   - Line numbers
   - Meaningful messages
   - Fail-fast behavior

5. Expression improvements (limited)
   - Controlled expressions inside trn (optional)

---

STRICT CONSTRAINTS

- DO NOT introduce VM or bytecode yet
- DO NOT redesign existing pipeline syntax
- DO NOT add too many keywords
- DO NOT break v0.2 programs
- Keep runtime fast and simple

---

VALIDATION TASKS

Analyze and answer:

1. Which features are essential for v0.3.0?
2. Which features should be postponed to v0.4 or v1.0?
3. Does adding functions break the current execution model?
4. What minimal function syntax fits the language philosophy?
5. How should modules be implemented without complexity?
6. What internal changes are required (parser, exec, symbol table)?
7. What risks exist (performance, complexity, bugs)?
8. What is the simplest possible implementation path?

---

OUTPUT REQUIRED

Provide:

1. Final v0.3 feature set (approved features only)
2. Feature priority order
3. Required code changes (high-level, file-wise)
4. Minimal syntax design (for functions/modules)
5. Risk analysis
6. Recommended implementation roadmap

---

DESIGN RULE

Minimal + Fast + Predictable > Feature-rich

---

Now design and validate a clean, realistic upgrade plan for Trionary v0.3.0.
